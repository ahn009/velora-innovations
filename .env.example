# Use a persistent production database. This SQLite value is development-only.
DATABASE_URL="file:./dev.db"

# Production origin used for canonical URLs, sitemap, robots, and social metadata.
# Confirm the final domain before deployment.
NEXT_PUBLIC_SITE_URL="https://velora-innovations.com"

# Recommended for production operations: receives a JSON notification after a lead has been stored.
# Without it, leads remain in the database but no team notification is sent.
LEAD_NOTIFICATION_WEBHOOK_URL=""

# Optional public contact details. When omitted, the site routes visitors to /contact.
NEXT_PUBLIC_CONTACT_EMAIL=""
NEXT_PUBLIC_LINKEDIN_URL=""
