'use client'

import { ArrowDown, ArrowRight, CalendarCheck2, Headphones, Home } from 'lucide-react'
import { Section, SectionHeading } from './section'
import { useConsultation } from './consultation-provider'

const examples = [
  { industry: 'Home Services', icon: Headphones, problem: 'After-hours service calls reach voicemail or arrive without enough detail.', system: 'An AI receptionist captures the issue, location, urgency, and preferred time, then checks service-area rules before routing.', outcome: 'A consistent intake path gives the team a structured lead and a clear exception route.' },
  { industry: 'Real Estate', icon: Home, problem: 'Property inquiries arrive across channels and agents spend time repeating first-step questions.', system: 'A chat or voice agent collects approved qualification details, checks consultation availability, and routes the inquiry to the right owner.', outcome: 'Each new inquiry can arrive with the context needed for a useful follow-up.' },
  { industry: 'Appointment-Based Teams', icon: CalendarCheck2, problem: 'Routine questions and scheduling requests interrupt the team throughout the day.', system: 'A scoped assistant answers approved questions, offers eligible slots, confirms the booking, and updates connected systems.', outcome: 'The team spends less time coordinating routine steps while retaining human control of exceptions.' },
] as const

export function ResultsSection({ compact = false }: { compact?: boolean }) {
  const { openConsultation } = useConsultation()
  return <Section id="results" background="muted"><SectionHeading label="Example Implementation" title="Inspect an illustrative workflow" description="This example shows how an implementation may be structured. Scope, integrations, and outcomes depend on the client’s actual systems and operating process." />
    <div className={`grid gap-5 ${compact ? 'lg:grid-cols-1 lg:max-w-3xl' : 'lg:grid-cols-3'}`}>{examples.slice(0, compact ? 1 : examples.length).map(({ industry, icon: Icon, problem, system, outcome }) => <article key={industry} className="flex h-full flex-col rounded-[var(--radius-lg)] border border-border-subtle bg-surface-primary p-5 shadow-soft sm:p-6"><div className="flex items-center justify-between gap-4"><span className="flex h-10 w-10 items-center justify-center rounded-[var(--radius-md)] bg-brand-primary/10 text-brand-hover"><Icon className="h-5 w-5" aria-hidden="true" /></span><span className="rounded-full bg-background-secondary px-2.5 py-1 text-[10px] font-semibold uppercase tracking-wider text-text-muted">Example workflow</span></div><h3 className="mt-5 text-xl font-semibold tracking-tight text-text-primary">{industry}</h3><div className="mt-5 space-y-4 text-sm leading-6"><div><p className="eyebrow">Problem</p><p className="mt-1 text-text-secondary">{problem}</p></div><ArrowDown className="h-4 w-4 text-brand-primary" aria-hidden="true" /><div><p className="eyebrow">Example system</p><p className="mt-1 text-text-secondary">{system}</p></div><ArrowDown className="h-4 w-4 text-brand-primary" aria-hidden="true" /><div className="rounded-[var(--radius-md)] bg-background-secondary p-3"><p className="eyebrow">Operational outcome</p><p className="mt-1 text-text-primary">{outcome}</p></div></div><button type="button" onClick={openConsultation} className="group mt-6 inline-flex min-h-10 items-center self-start text-sm font-semibold text-brand-hover focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-2">Discuss a similar workflow <ArrowRight className="ml-2 h-4 w-4 transition-transform group-hover:translate-x-0.5" aria-hidden="true" /></button></article>)}</div>
  </Section>
}
