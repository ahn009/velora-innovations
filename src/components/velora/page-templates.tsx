import Link from 'next/link'
import { ArrowRight, Check, CircleAlert, Home, ShieldCheck, Workflow } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Section, SectionHeading } from './section'
import { FaqSection } from './faq-section'
import { FinalCtaSection } from './final-cta-section'

export type SolutionContent = { eyebrow: string; title: string; description: string; outcome: string; does: string[]; doesNot: string[]; workflow: string[]; useCases: { title: string; description: string }[]; integrations: string[]; faq: { question: string; answer: string }[] }
export type IndustryContent = { eyebrow: string; title: string; description: string; problems: string[]; systems: { title: string; description: string }[]; conversation: { speaker: string; text: string }[]; workflow: string[]; integrations: string[]; considerations: string[]; faq: { question: string; answer: string }[] }

export const aiReceptionist: SolutionContent = { eyebrow: 'Solution · AI Receptionist', title: 'An AI receptionist built around the way your business handles calls', description: 'Answer routine enquiries, collect the right details, route qualified requests, and give your team a clear human handoff when the conversation needs judgment.', outcome: 'A defined first response for calls and enquiries without pretending the system replaces every reception responsibility.', does: ['Answer approved routine questions', 'Capture caller details and intent', 'Check configured service-area or eligibility rules', 'Offer eligible appointment windows where supported', 'Route calls and notify the right team member', 'Update connected systems with a useful summary'], doesNot: ['Invent unavailable services or appointments', 'Make regulated professional decisions', 'Bypass human approval for restricted actions', 'Guarantee bookings, revenue, or customer outcomes', 'Operate outside configured permissions or knowledge'], workflow: ['Customer call', 'AI receptionist', 'Intent + qualification', 'Business rules', 'Appointment, routing, or handoff', 'CRM update'], useCases: [{ title: 'Home services', description: 'Capture the issue, location, urgency, and timing before routing a service request.' }, { title: 'Dental practices', description: 'Handle approved questions, collect appointment intent, and route clinical or sensitive questions.' }, { title: 'Law firms', description: 'Collect administrative intake details and route matters without providing legal advice.' }, { title: 'Real estate', description: 'Capture enquiry context, qualify the request, and route to an available agent.' }, { title: 'Property management', description: 'Triage routine tenant or viewing enquiries with defined escalation rules.' }], integrations: ['Phone provider', 'CRM', 'Calendar', 'SMS or email', 'Team notifications'], faq: [{ question: 'Does an AI receptionist replace our front desk?', answer: 'No. It can handle defined repetitive work and improve first response, while people remain responsible for exceptions, sensitive conversations, judgment, and relationship management.' }, { question: 'Can it transfer a call to a person?', answer: 'Yes, when the selected channel and implementation support a defined transfer or notification path.' }, { question: 'Can it book appointments?', answer: 'Where a calendar provides suitable access and the business rules are clear, eligible appointments can be offered and confirmed.' }] }

export const homeServices: IndustryContent = { eyebrow: 'Industry · Home Services', title: 'AI automation for home-service businesses that need faster customer response', description: 'Turn missed calls, after-hours requests, and scheduling interruptions into a structured intake workflow your team can review and own.', problems: ['Calls arrive after hours or go to voicemail', 'Urgent requests need triage before the team responds', 'Technicians or coordinators repeat the same first-step questions', 'Leads arrive without service area, issue, or timing details', 'Follow-up is inconsistent when the day gets busy'], systems: [{ title: 'AI Receptionist', description: 'Answers approved routine calls, captures the service issue, and identifies when a human needs to take over.' }, { title: 'Lead Qualification', description: 'Checks service area, request type, urgency, and other agreed criteria before routing.' }, { title: 'Appointment Automation', description: 'Checks eligible availability and sends the right confirmation or team notification.' }], conversation: [{ speaker: 'Customer', text: 'My AC stopped working this afternoon. Do you service my area?' }, { speaker: 'AI agent', text: 'I can check that. What is the service address, and is the system completely without cooling?' }, { speaker: 'Next action', text: 'Service area check → urgency rule → eligible appointment or human escalation' }], workflow: ['Incoming call', 'AI receptionist', 'Issue + location + urgency', 'Service-area rules', 'Appointment or team routing', 'CRM lead + notification'], integrations: ['Phone system', 'CRM or job management', 'Calendar', 'SMS confirmation', 'Team notification'], considerations: ['Emergency or safety-related requests need a clear human escalation path.', 'The agent should not diagnose equipment or promise availability it cannot verify.', 'Service areas, hours, pricing rules, and appointment eligibility must come from approved business information.'], faq: [{ question: 'Can the agent handle emergency requests?', answer: 'It can identify an urgency signal and follow a configured escalation path, but it should not make safety or professional judgments on its own.' }, { question: 'Can it check our service area?', answer: 'Yes, if the business provides a reliable service-area rule or system access that can be checked.' }, { question: 'Can it schedule a technician?', answer: 'It can offer eligible availability when the calendar or job-management system provides suitable access and the scheduling rules are defined.' }] }

function WorkflowRail({ items }: { items: string[] }) { return <ol className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{items.map((item, index) => <li key={item} className="flex items-center gap-3 rounded-[var(--radius-md)] border border-border-subtle bg-surface-primary p-4 text-sm font-medium text-text-primary"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-brand-primary/10 text-xs text-brand-hover">{index + 1}</span>{item}</li>)}</ol> }

function TemplateHero({ eyebrow, title, description, label }: { eyebrow: string; title: string; description: string; label: string }) { return <section className="relative overflow-hidden border-b border-border-subtle bg-background-secondary"><div className="pointer-events-none absolute left-1/2 top-0 h-72 w-[48rem] -translate-x-1/2 rounded-full bg-brand-primary/10 blur-3xl" aria-hidden="true" /><div className="relative mx-auto max-w-7xl px-5 py-14 sm:px-8 sm:py-20 lg:px-10 lg:py-24"><p className="eyebrow">{eyebrow}</p><div className="mt-4 max-w-4xl"><h1 className="text-balance text-4xl font-semibold leading-tight tracking-tight text-text-primary sm:text-5xl lg:text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-base leading-7 text-text-secondary sm:text-lg">{description}</p><Button className="mt-8" variant="brand" asChild><Link href="/consultation">Discuss this {label} <ArrowRight className="h-4 w-4" aria-hidden="true" /></Link></Button></div></div></section> }

export function SolutionPageTemplate({ content }: { content: SolutionContent }) { return <main id="main-content"><TemplateHero eyebrow={content.eyebrow} title={content.title} description={content.description} label="workflow" /><Section background="muted"><div className="grid gap-10 lg:grid-cols-[0.75fr_1.25fr] lg:gap-16"><div><p className="eyebrow">Why it matters</p><h2 className="mt-3 text-3xl font-semibold tracking-tight text-text-primary">{content.outcome}</h2></div><p className="max-w-2xl text-lg leading-8 text-text-secondary">A useful agent is scoped around a real process: what it can know, what it can do, what it must not do, and who owns the exceptions.</p></div></Section><Section><SectionHeading align="left" label="Capabilities" title="What the agent can do—and where it stops" /><div className="grid gap-5 lg:grid-cols-2"><div className="rounded-[var(--radius-xl)] border border-brand-primary/25 bg-brand-primary/[0.04] p-6"><h3 className="text-xl font-semibold text-text-primary">Within scope</h3><ul className="mt-5 space-y-3">{content.does.map((item) => <li key={item} className="flex gap-2 text-sm leading-6 text-text-secondary"><Check className="mt-1 h-4 w-4 shrink-0 text-brand-primary" aria-hidden="true" />{item}</li>)}</ul></div><div className="rounded-[var(--radius-xl)] border border-border-subtle bg-surface-primary p-6"><h3 className="text-xl font-semibold text-text-primary">Clear boundaries</h3><ul className="mt-5 space-y-3">{content.doesNot.map((item) => <li key={item} className="flex gap-2 text-sm leading-6 text-text-secondary"><CircleAlert className="mt-1 h-4 w-4 shrink-0 text-amber-600" aria-hidden="true" />{item}</li>)}</ul></div></div></Section><Section background="muted"><SectionHeading label="Workflow" title="A visible path from conversation to action" description="The exact actions depend on systems, permissions, and business rules confirmed during implementation." /><WorkflowRail items={content.workflow} /></Section><Section><SectionHeading align="left" label="Use cases" title="Where this pattern can help" /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{content.useCases.map((item) => <article key={item.title} className="rounded-[var(--radius-lg)] border border-border-subtle bg-surface-primary p-5 shadow-soft"><h3 className="font-semibold text-text-primary">{item.title}</h3><p className="mt-2 text-sm leading-6 text-text-secondary">{item.description}</p></article>)}</div></Section><Section background="muted"><div className="grid gap-10 lg:grid-cols-2"><div><SectionHeading align="left" label="Integrations" title="Connected only where access is appropriate" description="Available integrations depend on the actual software, APIs, authentication, permissions, and data mapping." /></div><div className="grid gap-3 sm:grid-cols-2">{content.integrations.map((item) => <div key={item} className="flex items-center gap-3 rounded-[var(--radius-md)] border border-border-subtle bg-surface-primary p-4 text-sm font-medium text-text-primary"><Workflow className="h-4 w-4 text-brand-primary" aria-hidden="true" />{item}</div>)}</div></div></Section><FaqSection items={content.faq} /><FinalCtaSection /></main> }

export function IndustryPageTemplate({ content }: { content: IndustryContent }) { return <main id="main-content"><TemplateHero eyebrow={content.eyebrow} title={content.title} description={content.description} label="this workflow" /><Section background="muted"><SectionHeading label="Common problems" title="The operating friction teams recognize" /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{content.problems.map((problem) => <div key={problem} className="rounded-[var(--radius-lg)] border border-border-subtle bg-surface-primary p-5 text-sm leading-6 text-text-secondary">{problem}</div>)}</div></Section><Section><SectionHeading align="left" label="Recommended systems" title="Start with the customer moments that need structure" /><div className="grid gap-5 lg:grid-cols-3">{content.systems.map(({ title, description }, index) => <article key={title} className="rounded-[var(--radius-lg)] border border-border-subtle bg-surface-primary p-6 shadow-soft"><span className="eyebrow">0{index + 1}</span><h3 className="mt-4 text-xl font-semibold text-text-primary">{title}</h3><p className="mt-2 text-sm leading-6 text-text-secondary">{description}</p></article>)}</div></Section><Section background="muted"><div className="grid items-start gap-8 lg:grid-cols-[0.9fr_1.1fr] lg:gap-16"><div><SectionHeading align="left" label="Example conversation" title="Useful context before a team member gets involved" description="This is an illustrative scenario, not a claim about live production behavior." /></div><div className="space-y-3 rounded-[var(--radius-xl)] border border-border-subtle bg-surface-primary p-5 shadow-soft sm:p-6">{content.conversation.map(({ speaker, text }) => <div key={speaker} className="rounded-[var(--radius-md)] bg-background-secondary p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-brand-hover">{speaker}</p><p className="mt-1 text-sm leading-6 text-text-primary">{text}</p></div>)}</div></div></Section><Section><SectionHeading label="Example workflow" title="From request to a routed, reviewable record" /><WorkflowRail items={content.workflow} /></Section><Section background="muted"><div className="grid gap-10 lg:grid-cols-2"><div><SectionHeading align="left" label="Industry considerations" title="Boundaries matter in every customer interaction" /></div><ul className="space-y-4">{content.considerations.map((item) => <li key={item} className="flex gap-3 text-sm leading-6 text-text-secondary"><ShieldCheck className="mt-1 h-4 w-4 shrink-0 text-brand-primary" aria-hidden="true" />{item}</li>)}</ul></div><div className="mt-10 flex flex-wrap gap-3">{content.integrations.map((item) => <span key={item} className="inline-flex items-center gap-2 rounded-full border border-border-subtle bg-surface-primary px-3 py-2 text-xs font-semibold text-text-secondary"><Home className="h-3.5 w-3.5 text-brand-primary" aria-hidden="true" />{item}</span>)}</div></Section><FaqSection items={content.faq} /><FinalCtaSection /></main> }
